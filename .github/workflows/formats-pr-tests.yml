name: Radoub.Formats PR Test Suite

on:
  pull_request:
    branches: [main, develop]
    paths:
      - 'Radoub.Formats/**'
      - '.github/workflows/formats-pr-tests.yml'
  push:
    branches: [main]
    paths:
      - 'Radoub.Formats/**'
      - '.github/workflows/formats-pr-tests.yml'

jobs:
  test:
    strategy:
      fail-fast: false
      matrix:
        os: [windows-latest, ubuntu-latest]
    runs-on: ${{ matrix.os }}
    permissions:
      contents: read
      checks: write
      pull-requests: write

    steps:
    - name: Checkout code
      uses: actions/checkout@v6

    - name: Setup .NET
      uses: actions/setup-dotnet@v5
      with:
        dotnet-version: '9.0.x'

    - name: Restore dependencies
      run: dotnet restore Radoub.Formats.sln
      working-directory: ./Radoub.Formats

    - name: Build solution
      run: dotnet build Radoub.Formats.sln --configuration Release --no-restore
      working-directory: ./Radoub.Formats

    - name: Run tests with coverage
      shell: bash
      run: |
        dotnet test Radoub.Formats.Tests/Radoub.Formats.Tests.csproj \
          --configuration Release \
          --no-build \
          --verbosity normal \
          --logger "trx;LogFileName=test-results.trx" \
          --logger "console;verbosity=normal" \
          --collect:"XPlat Code Coverage" \
          --results-directory ./TestResults
      working-directory: ./Radoub.Formats

    - name: Upload test results
      if: always()
      uses: actions/upload-artifact@v6
      with:
        name: formats-test-results-${{ matrix.os }}
        path: ./Radoub.Formats/TestResults
        retention-days: 30

    - name: Test Report
      uses: dorny/test-reporter@v2
      if: always()
      with:
        name: Formats Test Results (${{ matrix.os }})
        path: './Radoub.Formats/TestResults/test-results.trx'
        reporter: dotnet-trx
        fail-on-error: true

    - name: Code Coverage Summary
      uses: irongut/CodeCoverageSummary@v1.3.0
      if: matrix.os == 'ubuntu-latest'
      with:
        filename: './Radoub.Formats/TestResults/**/coverage.cobertura.xml'
        badge: true
        format: markdown
        output: both
        thresholds: '50 75'

    - name: Add Coverage PR Comment
      uses: marocchino/sticky-pull-request-comment@v2
      if: github.event_name == 'pull_request' && matrix.os == 'ubuntu-latest'
      with:
        recreate: true
        path: code-coverage-results.md
