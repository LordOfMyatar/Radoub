<UserControl xmlns="https://github.com/avaloniaui"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:vm="using:Radoub.UI.ViewModels"
             mc:Ignorable="d" d:DesignWidth="400" d:DesignHeight="500"
             x:Class="Radoub.UI.Controls.EquipmentSlotsPanel"
             x:DataType="vm:EquipmentSlotViewModel">

  <UserControl.Styles>
    <!-- Equipment slot style -->
    <Style Selector="Border.equipment-slot">
      <Setter Property="Width" Value="64"/>
      <Setter Property="Height" Value="64"/>
      <Setter Property="BorderBrush" Value="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"/>
      <Setter Property="BorderThickness" Value="1"/>
      <Setter Property="CornerRadius" Value="4"/>
      <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundBaseLowBrush}"/>
      <Setter Property="Margin" Value="4"/>
      <Setter Property="Cursor" Value="Hand"/>
    </Style>

    <Style Selector="Border.equipment-slot:pointerover">
      <Setter Property="BorderBrush" Value="{DynamicResource SystemControlHighlightAccentBrush}"/>
      <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundBaseMediumLowBrush}"/>
    </Style>

    <Style Selector="Border.equipment-slot.selected">
      <Setter Property="BorderBrush" Value="{DynamicResource SystemControlHighlightAccentBrush}"/>
      <Setter Property="BorderThickness" Value="2"/>
    </Style>

    <Style Selector="Border.equipment-slot.has-item">
      <Setter Property="Background" Value="{DynamicResource SystemControlBackgroundListMediumBrush}"/>
    </Style>

    <Style Selector="Border.equipment-slot.has-warning">
      <Setter Property="BorderBrush" Value="{DynamicResource SystemFillColorCautionBrush}"/>
    </Style>

    <!-- Slot label style -->
    <Style Selector="TextBlock.slot-label">
      <Setter Property="FontSize" Value="10"/>
      <Setter Property="HorizontalAlignment" Value="Center"/>
      <Setter Property="Foreground" Value="{DynamicResource SystemControlForegroundBaseMediumLowBrush}"/>
    </Style>

    <!-- Warning icon style -->
    <Style Selector="TextBlock.warning-icon">
      <Setter Property="FontSize" Value="14"/>
      <Setter Property="Foreground" Value="{DynamicResource SystemFillColorCautionBrush}"/>
    </Style>

  </UserControl.Styles>

  <Grid RowDefinitions="Auto,*">
    <!-- Tab bar for Standard/Natural toggle -->
    <Border Grid.Row="0"
            Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
            Padding="4">
      <StackPanel Orientation="Horizontal" Spacing="8">
        <RadioButton x:Name="StandardTab"
                     Content="Standard Equipment"
                     IsChecked="True"
                     GroupName="EquipmentTabs"/>
        <RadioButton x:Name="NaturalTab"
                     Content="Natural (Creature)"
                     GroupName="EquipmentTabs"/>
      </StackPanel>
    </Border>

    <!-- Slot panels container -->
    <Grid Grid.Row="1">
      <!-- Standard equipment slots panel - compact paperdoll layout -->
      <ScrollViewer x:Name="StandardPanel" IsVisible="True">
        <Grid Margin="4" HorizontalAlignment="Center" VerticalAlignment="Top">
          <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>  <!-- Row 0: Head, Cloak -->
            <RowDefinition Height="Auto"/>  <!-- Row 1: Chest, Neck, Arms -->
            <RowDefinition Height="Auto"/>  <!-- Row 2: Weapons, Belt -->
            <RowDefinition Height="Auto"/>  <!-- Row 3: Boots, Ammo, Rings -->
          </Grid.RowDefinitions>
          <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
          </Grid.ColumnDefinitions>

          <!-- Row 0: Head (left-center), Cloak (right-center) -->
          <StackPanel Grid.Row="0" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <ContentControl x:Name="SlotHead" Content="{Binding}"/>
          </StackPanel>
          <StackPanel Grid.Row="0" Grid.Column="2" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Bottom">
            <ContentControl x:Name="SlotCloak" Content="{Binding}"/>
          </StackPanel>

          <!-- Row 1: Chest (left), Neck (center), Arms (right) -->
          <StackPanel Grid.Row="1" Grid.Column="0" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ContentControl x:Name="SlotChest" Content="{Binding}"/>
          </StackPanel>
          <StackPanel Grid.Row="1" Grid.Column="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ContentControl x:Name="SlotNeck" Content="{Binding}"/>
          </StackPanel>
          <StackPanel Grid.Row="1" Grid.Column="3" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ContentControl x:Name="SlotArms" Content="{Binding}"/>
          </StackPanel>

          <!-- Row 2: Right Hand (left), Belt (center), Left Hand (right) -->
          <StackPanel Grid.Row="2" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Top">
            <ContentControl x:Name="SlotRightHand" Content="{Binding}"/>
          </StackPanel>
          <StackPanel Grid.Row="2" Grid.Column="1" Grid.ColumnSpan="2" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ContentControl x:Name="SlotBelt" Content="{Binding}"/>
          </StackPanel>
          <StackPanel Grid.Row="2" Grid.Column="3" HorizontalAlignment="Center" VerticalAlignment="Top">
            <ContentControl x:Name="SlotLeftHand" Content="{Binding}"/>
          </StackPanel>

          <!-- Row 3: Boots (left), Arrows+Bullets+Bolts (center), Rings (right) -->
          <StackPanel Grid.Row="3" Grid.Column="0" HorizontalAlignment="Center" VerticalAlignment="Center">
            <ContentControl x:Name="SlotBoots" Content="{Binding}"/>
          </StackPanel>
          <StackPanel Grid.Row="3" Grid.Column="1" Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="2">
            <ContentControl x:Name="SlotArrows" Content="{Binding}"/>
            <ContentControl x:Name="SlotBullets" Content="{Binding}"/>
            <ContentControl x:Name="SlotBolts" Content="{Binding}"/>
          </StackPanel>
          <StackPanel Grid.Row="3" Grid.Column="3" HorizontalAlignment="Center" VerticalAlignment="Center" Spacing="2">
            <ContentControl x:Name="SlotLeftRing" Content="{Binding}"/>
            <ContentControl x:Name="SlotRightRing" Content="{Binding}"/>
          </StackPanel>
        </Grid>
      </ScrollViewer>

      <!-- Natural equipment slots panel (creature-only) -->
      <ScrollViewer x:Name="NaturalPanel" IsVisible="False">
        <StackPanel Margin="8" Spacing="8" HorizontalAlignment="Center">
          <TextBlock Text="Creature Natural Equipment"
                     HorizontalAlignment="Center"
                     FontWeight="SemiBold"
                     Margin="0,0,0,8"/>

          <WrapPanel HorizontalAlignment="Center">
            <ContentControl x:Name="SlotClaw1" Content="{Binding}"/>
            <ContentControl x:Name="SlotClaw2" Content="{Binding}"/>
            <ContentControl x:Name="SlotClaw3" Content="{Binding}"/>
          </WrapPanel>

          <ContentControl x:Name="SlotSkin" Content="{Binding}" HorizontalAlignment="Center"/>
        </StackPanel>
      </ScrollViewer>
    </Grid>
  </Grid>
</UserControl>
