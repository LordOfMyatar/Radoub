namespace Radoub.Formats.Logging;

/// <summary>
/// Log levels for filtering log output.
/// Lower values = more severe, higher values = more verbose.
/// </summary>
public enum LogLevel
{
    ERROR = 0,
    WARN = 1,
    INFO = 2,
    DEBUG = 3,
    TRACE = 4
}
